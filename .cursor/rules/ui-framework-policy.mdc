# UI Framework 使用政策

**制定日期**：2025-10-19  
**最後更新**：2025-10-19  
**政策版本**：1.0

---

## 🎯 政策目標

本專案為確保 UI 一致性、可維護性和效能，制定統一的 UI Framework 使用政策。

---

## 📋 強制規範

### ✅ **唯一指定 UI Framework：shadcn/ui**

本專案 **僅允許** 使用 **shadcn/ui** 作為 UI 組件庫，其他 UI Framework 一律
**禁止使用**。

#### shadcn/ui 優勢

1. **基於 Radix UI Primitives**

   - 提供完整的無障礙支援 (Accessibility)
   - 符合 WAI-ARIA 標準
   - 鍵盤導航完整支援

2. **完全可自訂**

   - 組件代碼直接在專案中，可完全控制
   - 與 Tailwind CSS 完美整合
   - 無需覆蓋第三方樣式

3. **無執行時依賴**

   - 不會增加 bundle size
   - 只引入實際使用的組件
   - 效能最佳化

4. **符合專案設計系統**
   - 可完全客製化為專案風格
   - 支援設計 token 系統
   - 易於維護和更新

---

## 🚫 禁止使用的 UI Framework

以下 UI Framework **嚴格禁止** 在本專案中使用：

### ❌ Material-UI (MUI)

- **問題**：樣式覆蓋複雜、bundle size 大
- **替代方案**：使用 shadcn/ui 對應組件

### ❌ Ant Design

- **問題**：設計風格固定、難以客製化
- **替代方案**：使用 shadcn/ui 對應組件

### ❌ Chakra UI

- **問題**：與 Tailwind CSS 衝突
- **替代方案**：使用 shadcn/ui 對應組件

### ❌ Bootstrap / React-Bootstrap

- **問題**：與專案設計系統不符
- **替代方案**：使用 shadcn/ui + Tailwind CSS

### ❌ 其他第三方 UI 組件庫

- 除非經過架構審查並獲得批准

---

## 📦 shadcn/ui 組件清單

本專案目前已安裝的 shadcn/ui 組件：

### 基礎組件

- ✅ **Button** - 按鈕組件
- ✅ **Input** - 輸入框組件
- ✅ **Textarea** - 多行文字輸入框
- ✅ **Checkbox** - 勾選框組件

### 表單組件

- ✅ **Select** - 下拉選單組件（基於 Radix UI Select）

### 反饋組件

- ✅ **Dialog** - 對話框組件
- ✅ **Drawer** - 抽屜組件

### 互動組件

- ✅ **Slider** - 滑桿組件

### 自訂組件

- ✅ **ImageSlider** - 圖片輪播組件

---

## 🔧 如何使用 shadcn/ui

### 1. 安裝新組件

```bash
# 使用 shadcn CLI 安裝組件
npx shadcn@latest add [component-name]

# 例如：安裝 Card 組件
npx shadcn@latest add card
```

### 2. 組件位置

所有 shadcn/ui 組件位於：

```
src/components/ui/
├── button.jsx
├── checkbox.jsx
├── dialog.jsx
├── drawer.jsx
├── input.jsx
├── select.jsx
├── slider.jsx
└── textarea.jsx
```

### 3. 使用方式

```jsx
// ✅ 正確：使用 shadcn/ui 組件
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'

function MyComponent() {
  return (
    <div>
      <Input placeholder="請輸入..." />
      <Select>
        <SelectTrigger>
          <SelectValue placeholder="請選擇" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="1">選項 1</SelectItem>
          <SelectItem value="2">選項 2</SelectItem>
        </SelectContent>
      </Select>
      <Button>提交</Button>
    </div>
  )
}
```

### 4. 客製化組件

```jsx
// ✅ 正確：基於 shadcn/ui 組件進行客製化
import { Button } from '@/components/ui/button'
import { cn } from '@/lib/utils'

function CustomButton({ className, children, ...props }) {
  return (
    <Button
      className={cn(
        'bg-blue-primary hover:bg-blue-primary/90',
        'font-anton tracking-widest',
        className,
      )}
      {...props}
    >
      {children}
    </Button>
  )
}
```

---

## 🔄 React Hook Form 整合

### shadcn/ui + React Hook Form 包裝組件

為了與 React Hook Form 整合，需建立包裝組件：

```jsx
// src/components/common/hook-form/rhf-select.jsx
'use client'

import { Controller, useFormContext } from 'react-hook-form'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'

export function RHFSelect({ name, options, placeholder, ...props }) {
  const { control } = useFormContext()

  return (
    <Controller
      name={name}
      control={control}
      render={({ field }) => (
        <Select value={field.value} onValueChange={field.onChange}>
          <SelectTrigger {...props}>
            <SelectValue placeholder={placeholder} />
          </SelectTrigger>
          <SelectContent>
            {options.map(option => (
              <SelectItem key={option.value} value={option.value}>
                {option.label}
              </SelectItem>
            ))}
          </SelectContent>
        </Select>
      )}
    />
  )
}
```

---

## 🚨 違規處理

### 程式碼審查時發現違規

1. **立即停止合併**
2. **要求開發者重構**
3. **使用 shadcn/ui 對應組件替換**

### 遺留程式碼處理

若發現專案中存在其他 UI Framework 的遺留程式碼：

#### 優先級 1：立即重構

- **範圍**：新功能頁面
- **期限**：開發完成前必須重構

#### 優先級 2：計劃重構

- **範圍**：現有功能頁面
- **期限**：下一次功能更新時重構

#### 優先級 3：技術債務追蹤

- **範圍**：核心功能頁面
- **期限**：加入重構計劃，逐步遷移

---

## 📊 遷移指南

### 從 Material-UI 遷移到 shadcn/ui

| Material-UI                   | shadcn/ui                       | 說明       |
| ----------------------------- | ------------------------------- | ---------- |
| `<TextField />`               | `<Input />`                     | 基本輸入框 |
| `<Select />` + `<MenuItem />` | `<Select />` + `<SelectItem />` | 下拉選單   |
| `<Button />`                  | `<Button />`                    | 按鈕組件   |
| `<Checkbox />`                | `<Checkbox />`                  | 勾選框     |
| `<Dialog />`                  | `<Dialog />`                    | 對話框     |
| `<Drawer />`                  | `<Drawer />`                    | 抽屜       |
| `<Slider />`                  | `<Slider />`                    | 滑桿       |

### 遷移步驟

1. **識別現有 MUI 組件**

   ```bash
   # 搜尋所有使用 MUI 的檔案
   grep -r "from '@mui/material'" src/
   ```

2. **安裝對應的 shadcn/ui 組件**

   ```bash
   npx shadcn@latest add [component-name]
   ```

3. **替換 import 和使用方式**

   ```jsx
   // ❌ 舊的 MUI 方式
   import { TextField, MenuItem, Select } from '@mui/material'

   // ✅ 新的 shadcn/ui 方式
   import { Input } from '@/components/ui/input'
   import {
     Select,
     SelectContent,
     SelectItem,
     SelectTrigger,
     SelectValue,
   } from '@/components/ui/select'
   ```

4. **更新樣式**

   - 移除 MUI 的 `sx` prop
   - 使用 Tailwind CSS className
   - 使用 `cn()` 工具函數合併樣式

5. **測試功能**
   - 確保所有功能正常運作
   - 驗證無障礙功能
   - 檢查響應式設計

---

## ✅ 檢查清單

在提交程式碼前，請確認：

- [ ] 沒有引入任何新的 UI Framework
- [ ] 所有 UI 組件都使用 shadcn/ui
- [ ] 樣式使用 Tailwind CSS
- [ ] 已移除 MUI 相關的 import
- [ ] 組件功能正常運作
- [ ] 無障礙功能正常
- [ ] 響應式設計正確

---

## 📚 參考資源

- [shadcn/ui 官方文檔](https://ui.shadcn.com/)
- [Radix UI 文檔](https://www.radix-ui.com/)
- [Tailwind CSS 文檔](https://tailwindcss.com/)
- [React Hook Form 文檔](https://react-hook-form.com/)

---

## 🤝 例外申請

若有特殊需求需要使用其他 UI Framework，必須：

1. **提出書面申請**

   - 說明為什麼 shadcn/ui 無法滿足需求
   - 提供技術評估報告
   - 評估對專案的影響

2. **技術審查**

   - 架構師審查
   - 團隊討論
   - 效能評估

3. **批准後**
   - 記錄在專案文檔中
   - 限定使用範圍
   - 定期重新評估

---

**政策制定者**：Paper Hsiao (@paperhsiaooo)  
**政策執行者**：全體開發團隊  
**政策監督者**：專案架構師
