# AI 開發工作流程規則

## 🎯 UI 驗證規則（強制執行）

**核心規則**：當使用者提出任何與 UI 相關的問題時，AI 必須使用 Playwright
MCP 工具進行實際驗證。

### 適用情境

- 樣式問題（文字大小、padding、顏色等）
- 佈局問題（對齊、間距、響應式設計）
- 互動問題（按鈕點擊、表單驗證、錯誤顯示）
- 視覺效果（動畫、過渡效果、hover 狀態）
- 跨頁面一致性比較

### 工作流程

1. 使用 Playwright 導航到相關頁面
2. 截圖當前狀態
3. 分析問題並修改程式碼
4. 再次使用 Playwright 驗證
5. 截圖確認修復效果

## ⚠️ 程式碼提交前強制檢查

所有程式碼修改完成後，必須執行以下檢查：

1. **Lint 檢查**

   ```bash
   yarn lint
   ```

   - 必須通過（Exit code: 0）
   - 可以有 Warnings，但不能有 Errors

2. **Build 檢查**

   ```bash
   yarn build
   ```

   - 必須成功建置
   - 驗證所有 import 路徑正確
   - 檢查 Server/Client Component 使用正確

3. **不可跳過檢查**
   - 絕對不可以跳過 lint 或 build 就提交
   - 假設「應該沒問題」是不允許的

## 🎨 UI Framework 政策

**只允許使用 shadcn/ui**

- ✅ 允許：shadcn/ui（基於 Radix UI primitives）
- ❌ 禁止：Material-UI、Ant Design、Chakra UI 等其他 UI Framework

## 📚 詳細文檔

完整的開發規範請參考：

- `docs/AI_DEVELOPMENT_WORKFLOW.md` - AI 開發工作流程指南
- `docs/UI_FRAMEWORK_POLICY.md` - UI Framework 使用政策
- `docs/MIGRATION_FROM_MUI.md` - Material-UI 遷移計劃

## 🔧 技術堆疊

- Next.js 15 (App Router)
- React 19
- Tailwind CSS 4
- shadcn/ui (唯一指定 UI Framework)
- Zustand (狀態管理)
- React Query (資料緩存)
- React Hook Form + Zod (表單處理)
